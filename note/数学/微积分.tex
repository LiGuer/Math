* 微积分
	* 极限
		* 数列极限
			\Define
				$\lim_{n \to \infty} x_n=a <=> |x_n-a|<ε, for\ \forall ε>0, \ex_ists N \in Z_+, n > N$

			\Property
				- 极限唯一性
				- 收敛数列有界性
				- 收敛数列保号性

		* 函数极限

	* 微分
		* 一元函数微分
			\Define{导数}
				$
					\/{df}{dx} &= lim_{x \to 0}  \/{ f(x + Δx) - f(x - Δx) }{ 2 Δx }  \tag{一阶导}
					\/{d^n f}{dx^n} &= lim_{x \to 0} \/{ f^{(n - 1)}(x + Δx) - f^{(n - 1)}(x - Δx)} { 2 Δx }  \tag{n阶导}
				$

		* 多元函数微分
			\Define{偏导数}
				$\/{∂f}{∂x_i} = \/{f(...,x_i + Δx_i,...) -  f(..., x_i - Δx_i, ...)}{2 Δx_i}  \tag{一阶偏导}$
				$
					∂^2 f/∂x_i^2  = \/{f'(..,x_i+Δx_i,..) -  f'(..,x_i-Δ,..)}{Δx_i} = \/{f(..,x_i+Δx_i) - 2·f'(..,x) + f'(..,x_i-Δx_i)}{Δx_i^2}
				$

			* 梯度, 散度, 旋度
				\Define{梯度} 
					$\nabla f = \sum_i \/{∂f}{∂x_i} \hat{\.x_i}  \tag{直角坐标系}$ 
					矢量, 函数在该点处变化率最大的方向.

				\Define{散度} 
					$\nabla · \.f = \sum_i \/{∂f_{x_i}}{∂x_i}  \tag{直角坐标系}$ 
					标量, 矢量场在该点发散的程度, 表征场的有源性(>0源,<0汇,=0无源)

				\Define{旋度}
					$
						\nabla × \.f &= (∂fz/∂y - ∂fy/∂z) \.x
						&+ (∂fx/∂z - ∂fz/∂x) \.y
						&+ (∂fy/∂x - ∂fx/∂y) \.z  \tag{三维直角坐标系}$ 
					$
					矢量, 矢量场在该点旋转的程度, 方向是旋转度最大的环量的旋转轴, 旋转的方向满足右手定则, 大小是绕该旋转轴旋转的环量与旋转路径围成的面元面积之比.

	* 积分
		* 一元函数微分

		* 多元函数积分
			* 重积分