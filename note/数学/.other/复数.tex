* 复数
	\def
		复数是一对有序的实数$(a, b)$, 复数集合 $\.C = \{ (a, b) | a , b \in \.R$.
		因为$(0, 1) = \i$虚数单元, 所以复数也记作 $z = a + b · \i$, 且$\i · \i = -1$.

	- 运算
		- 模、幅角 
			$
				r = |z| = \sqrt{a^2 + b^2}
				θ = arg\ z = arctan(b/a)
			$
			因此复数也可以表示为
			$z = a + b · \i = r (\cos θ + \i · \sin θ) = r e^{\i · θ}$
		- 加 $z_1 + z_2 = (a + b · \i) + (c + d · \i) = (a + b) + (c + d) · \i$
		- 减 $z_1 - z_2 = (a - b · \i) + (c - d · \i) = (a - b) + (c - d) · \i$
		- 乘 $z_1 · z_2 = (a - b · \i) · (c - d · \i) = (a c - b d) + (a d + b c) · \i$
		- 除 $\/{ z_1 }{ z_2 } = \/{a - b · \i}{c - d · \i} = \/{a c + b d}{c^2 + d^2} + \/{b c + a d}{c^2 + d^2} · \i$
		- 共轭 $\bar z = a + (-b) · \i$
		- 幂  
			\Theorem{De Moiver公式}
				$z^p = r^p (\cos θ + \i · \sin θ)^p = r^p (\cos (p · θ) + \i · \sin(p · θ))$

			$z_1^{z_2} = $

	- 初等函数
		- 指数函数
			$e^z = e^{a + b · \i} = e^a (\cos a + \i · \sin b)$
		- 对数函数
			$\ln(z) = \ln(r e^{\i · θ}) = \ln(r) + \i ·θ$
		- 三角函数、双曲函数
			$
				\cos z  = \/{e^{\i · z} + e^{\i · z}}{2}
				\sin z  = \/{e^{\i · z} - e^{\i · z}}{2 · \i}
				\sinh z = \/{e^{z} - e^{-z} }{2}
				\cosh z = \/{e^{z} + e^{-z} }{2}
			$
			